use crate::setting::*;
use crate::utils::*;
use crate::NanometersApp;
use egui::*;

impl NanometersApp {
    pub fn gpu_test_meter(&mut self, rect: eframe::epaint::Rect, ui: &mut Ui) {
        // ui.painter().rect_filled(rect, 0.0, Color32::BLACK);
    }
}
